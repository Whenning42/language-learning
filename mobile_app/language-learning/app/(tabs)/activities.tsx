import { ThemedText } from '@/components/ThemedText';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function ActivityLogScreen() {
  return (
    <SafeAreaView>
      <ThemedText>TODO: Activities Log Screen</ThemedText>
    </SafeAreaView>
  )
}
